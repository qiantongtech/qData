---
description: 项目架构与目录（api/assets/components/layout/router/store/views 等）
globs: src/**/*
alwaysApply: false
---
# 项目架构

## 目录职责
- **api/**：接口定义，按业务模块分目录（小驼峰或模块简称）；每模块单独目录，文件名小驼峰、不超过 3 个单词。同目录下可放 **mock 文件**（`xxx.mock.js`），仅测试/开发使用，见 20-testing。
- **assets/**：静态资源；images/json/file/icons 按模块或 common 分子目录；**styles/** 放全局样式（base、variables、模块通用）。
- **components/**：通用 UI 组件，目录**大驼峰**、文件**小驼峰**；业务单页专用放 `views/xxx/components/`。
- **composables/**：组合式函数（如 useTableData、useDialog、useRequest、usePagination）。
- **directive/**：自定义指令（权限、复制、焦点等）。
- **layout/**：页面框架（导航栏、侧边栏、标签页等），与项目默认布局一致。
- **plugins/**：插件集成（auth、cache、download、modal、tab 等）。
- **router/**：路由按业务模块分子目录，文件小驼峰、统一 `index.js`；path 与目录结构一致。
- **store/**：Pinia 按领域划分（如 system、user）。
- **utils/**：纯工具函数，与 Vue 无关、不依赖 store/router。
- **views/**：页面视图，**层级与菜单一致**（一级菜单→二级→三级→功能页）；列表页 `index.vue`，新增页 `add/index.vue`，详情页 `detail/index.vue`，小驼峰+功能名。

## 引用边界
- **禁止** 跨层反向引用：views 不能被 api/utils 引用；store 不依赖具体 view 或组件。
- 允许：views → components / api / store / utils；api → utils（如 request）；components → utils。

## 与后端关系
- 前端仅通过 HTTP 调用后端；权限、菜单、字典等以接口约定为准，见 31-api-contract。
