---
description: 样式（选择器、BEM、变量、禁止内联、书写顺序）
globs: **/*.vue, **/*.scss, **/*.css
alwaysApply: false
---
# 样式体系

## 选型与作用域
- 以 **SCSS** 为主；单文件组件使用 `<style lang="scss" scoped>`，避免全局污染；需覆盖 Element Plus 时用类名包裹或 `:deep()`。

## 选择器
- **避免 ID 选择器**（`#id`），以类选择器为主；**减少标签选择器**，组件内优先局部类名；**层级深度不超过 3 层**。

## 选择器命名
- **小写 + 连字符**（kebab-case）；推荐 **BEM**：`block__element--modifier`；组件相关样式前缀与组件名一致（如 `.meta-detail`、`.meta-list`）。

## 缩进与格式
- **缩进 2 空格**；花括号独占一行，属性一行一个；颜色、长度单位统一（如 `#RRGGBB`、`rem/px`）。

## 变量与禁止内联
- **间距、颜色**使用全局变量（如 `var(--sp-8)`、`var(--primary)`），禁止在组件内写“魔法数”；**禁止在模板中写 `style="..."`**，动态样式用类名切换或计算属性。

## 书写顺序（从外到内）
1. 布局：display / position / top|right|bottom|left / z-index / flex|grid
2. 盒模型：width|height / margin|padding / border / box-sizing
3. 视觉：background / box-shadow / opacity / filter
4. 文本：font-* / line-height / text-* / color
5. 其他：transition / animation / cursor / pointer-events
