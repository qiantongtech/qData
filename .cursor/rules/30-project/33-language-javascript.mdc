---
description: JS 规范（命名、早返回、JSDoc、TODO/FIXME）
globs: **/*.js, **/*.vue
alwaysApply: false
---
# JavaScript 规范

## 命名
- **变量**：小驼峰（camelCase），如 `userName`、`tableData`、`isLoading`。
- **常量**：全大写 + 下划线（UPPER_SNAKE_CASE），如 `API_URL`、`MAX_RETRY_COUNT`。
- **方法**：动词开头、语义明确，如 `getList()`、`handleClick()`、`resetQuery()`。

## 书写习惯
- 使用 **let/const**，**禁止 var**；使用 **ES 模块**（import/export）；异步优先 `async/await`。
- **禁止对函数参数重新赋值**（`no-param-reassign`）；若需修改，先赋给局部变量再改。
- **逻辑控制**：尽量避免多层嵌套 if，使用**早返回（early return）** 提升可读性。

## 注释
- **单行**：`//` 放代码上方，行尾注释与代码间隔两空格；禁止无意义注释。
- **多行**：`/* ... */` 每行前加 `*`，注释块前后空一行。
- **函数**：使用 **JSDoc**，注明 `@param`、`@returns`，必要时 `@throws`、`@example`。
- **文件头**：模块/工具文件注明 `@file`、`@description`，可选 `@author`、`@created`。
- **特殊标识**：`TODO`、`FIXME`、`NOTE` 须附带责任人或时间，禁止“改下”“待定”等模糊用语。

## 与 Vue 的配合
- 组件 `props` 用 `defineProps()` 声明，必填/默认值写清楚；复杂结构用 JSDoc 或注释说明。
- `emit` 用 `defineEmits()` 声明事件名，载荷结构在注释中约定。
